# Slope Field Applet

Upload these files to the root of the `Slope-Field` repository.

The file `index.html` is the applet. It must replace the current repository `index.html`, which is not the applet.

